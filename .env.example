# Hosted Supabase project. Nothing in this project runs locally: a build that
# depends on one machine being reachable is not shippable, and a release build
# inlines these values at bundle time.
EXPO_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=replace-with-project-publishable-key
EXPO_PUBLIC_APP_ENV=staging

# SUPABASE_DB_URL is deliberately NOT here. It is the database connection
# string, it contains a password, and npm does not load this file for scripts
# anyway. Export it in your shell for `npm run db:test`:
#
#   export SUPABASE_DB_URL="postgresql://postgres:...@db.<ref>.supabase.co:5432/postgres"
#
# Dashboard: Project Settings -> Database -> Connection string (URI).
